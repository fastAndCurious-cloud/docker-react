sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t simplymodi/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true simplymodi/docker-react npm run test -- --coverage
